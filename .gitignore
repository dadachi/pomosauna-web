.playwright-mcp/
docs-private/
*.png
# keep the social-share image (a real asset, not a scratch screenshot)
!og-preview.png
# keep the favicon / touch-icon PNGs (real tracked assets)
!apple-touch-icon.png
!favicon-32.png
# keep the README hero screenshots (real tracked assets)
!screenshot-sauna.png
!screenshot-water.png
.gstack/
